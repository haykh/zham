# All heavy/mutable state is localized here — never commit it
.cache/

# Gradle / build outputs
.gradle/
build/
app/build/

# Local SDK pointer (we use ANDROID_SDK_ROOT from the nix shell instead)
local.properties

# Gradle wrapper jar is generated by `gradle wrapper`
# (kept out so we don't commit a binary; remove this line if you prefer to vendor it)
gradle/wrapper/gradle-wrapper.jar

# direnv
.direnv/

# devenv
.devenv/
.devenv.flake.nix
devenv.local.nix

CLAUDE.md
INSTRUCTIONS.md
PLAN.md
