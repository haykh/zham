# All heavy/mutable state is localized here — never commit it
.cache/

# Gradle / build outputs
.gradle/
build/
app/build/

# Local SDK pointer (we use ANDROID_SDK_ROOT from the nix shell instead)
local.properties

# NOTE: gradle/wrapper/gradle-wrapper.jar is intentionally committed (vendored)
# so a fresh clone — and CI (GitHub Actions) — can build without bootstrapping Gradle.

# direnv
.direnv/

# devenv
.devenv/
.devenv.flake.nix
devenv.local.nix

CLAUDE.md
INSTRUCTIONS.md
PLAN.md

# Release signing — never commit the keystore or its base64
zham-release.keystore
*.keystore
*.jks
*.keystore.b64
keystore.properties
